---
layout: null
sitemap:
  exclude: 'yes'
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  <!-- Posts -->
  {% for post in site.posts %}
    {% unless post.published == false %}
    <url>
      <loc>{{ site.url }}{{ post.url }}</loc>
      <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
      <changefreq>daily</changefreq>
      <priority>0.5</priority>
    </url>
    {% endunless %}
  {% endfor %}

  <!-- Pages -->
  {% for page in site.pages %}
    {% unless page.sitemap.exclude == "yes" or page.url contains "assets" %}
    <url>
      <loc>{{ site.url }}{{ page.url | remove: "index.html" }}</loc>
      <lastmod>{{ page.date | default: site.time | date_to_xmlschema }}</lastmod>
      <changefreq>daily</changefreq>
      <priority>0.5</priority>
    </url>
    {% endunless %}
  {% endfor %}

  <!-- collections -->
  {% assign collections_list = site.collections | where_exp: "col", "col.label != 'posts'" %}
  {% for collection in collections_list %}
    {% for doc in collection.docs %}
      {% unless doc.sitemap.exclude == "yes" %}
      <url>
        <loc>{{ site.url }}{{ doc.url }}</loc>
        <lastmod>{{ doc.date | default: site.time | date_to_xmlschema }}</lastmod>
        <changefreq>daily</changefreq>
        <priority>0.5</priority>
      </url>
      {% endunless %}
    {% endfor %}
  {% endfor %}
</urlset>
